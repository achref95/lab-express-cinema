<div id="single-cont">
    <img src="{{image}}" alt="">
    <div>
        <h1><a href="/movies">Go back</a></h1>
        <h2>{{title}}</h2>
        <h3>Director: {{director}}</h3>
        <h3>Stars</h4>
        
        <ul>
            {{#each stars}}
            <li>{{this}}</li>
            {{/each}}
        </ul>
        <p>{{description}}</p>
        <p>
            {{#each showtimes}}
            | {{this}} |
            {{/each}}
        </p>
    </div>
    </div>